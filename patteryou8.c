#include <stdio.h>
int main()
{
    int i, j, k, n;
    printf("enter the value of the n");
    scanf("%d", &n);
    int a[90][90];
    int end = 2 * n - 1;
    for (i = 1; i <= end; i++)
    {
        for (j = 1; j <= end; j++)
        {
            if (i == 1 || i == end || j == 1 || j == end)
            {
                a[i]
            }
        }
        printf("\n");
    }
}